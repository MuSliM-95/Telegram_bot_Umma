import { bot } from "../../bot-commands/commands.js"

export const chatInfo = async (id:string):Promise<boolean> => {
    try {
        // bot.telegram.send
    } catch (error) {
        
    }
    return true
}